# Vite dev-server config. Copy to `.env` for local overrides.
# In CI / deployment pipelines, set these as pipeline variables.

# Port the Vite dev server binds to. Must match Playwright baseURL.
WEB_PORT=5173

# Origin the dev server proxies /api/* to (the Hono API).
API_PROXY_TARGET=http://localhost:3001
